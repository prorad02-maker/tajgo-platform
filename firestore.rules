rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Demo rule: anonymous Firebase Auth is still authentication.
    // Replace with role- and ownership-based rules before public testing.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
